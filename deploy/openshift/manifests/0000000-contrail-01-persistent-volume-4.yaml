apiVersion: v1
kind: PersistentVolume
metadata:
  name: 4-pv
spec:
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 5Gi
  local:
    path: /mnt/volumes/4
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: node-role.kubernetes.io/master
              operator: Exists
  persistentVolumeReclaimPolicy: Retain
  